from setuptools import setup

setup(name='neural_layout',
      version='0.1',
      description='Use multi-level force layout method to visualize neural networks',
      author='Vincent Herrmann',
      license='MIT',
      packages=['neural_layout'],
      zip_safe=False)